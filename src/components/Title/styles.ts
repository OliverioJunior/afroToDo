import styled from 'styled-components';

export const ContainerTitle = styled.div`
  height: 4.5rem;
  width: min-content;
  color: var(--white);
  display: inherit;
  align-items: center;
  img {
    width: 1.5rem;
    height: 1.238rem;
  }
  h1 {
    font-size: 1.5rem;
    margin-left: 0.5rem;
  }
`;
