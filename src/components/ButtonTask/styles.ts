import styled from 'styled-components';

export const Container = styled.button`
  width: 2rem;
  height: 2rem;
  background: var(--gray-100);
  border: none;
  border-radius: 8px;
  svg {
    padding: 0.625rem;
  }
`;
